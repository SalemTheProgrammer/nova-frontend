# Base URL for the backend API. In dev, Vite proxies /api to localhost:8000,
# so the default works out of the box. Set this for production builds.
VITE_API_BASE=/api/v1

# API key sent as the X-API-Key header (must match the backend's API_KEYS).
VITE_API_KEY=dev-local-key
